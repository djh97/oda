# LLM fine-tuning dataset (synthetic)

This folder contains the synthetic JSONL dataset used for fine-tuning the donor–recipient matching LLM.
No real patient data are included.

- Format: JSON Lines (one example per line)
- Purpose: supervised fine-tuning / validation of structured match decisions + rationale
